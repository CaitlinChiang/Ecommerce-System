import { createTheme } from '@mui/material/styles'
import { red } from '@mui/material/colors'

export default createTheme({
  palette: {
    primary: { main: '#A88238' },
    secondary: { main: '#794618' },
    error: red
  }
})
