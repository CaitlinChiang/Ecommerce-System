import { ReactElement } from 'react'
import { Typography } from '@mui/material'

const NoPermissions = (): ReactElement => {
  return <Typography>{'No access permissions.'}</Typography>
}

export default NoPermissions
