type AuditLog {
  _id: ID
  action: String
  orderId: ID
  paymentId: ID
  productId: ID
  productVariantId: ID
  userId: ID
  createdAt: String
  createdBy: ID
}

extend type Query {
  get_audit_logs(
    action: String
    dateRange: DateRangeInput
    orderId: ID
    paginateData: PaginateDataInput
    paymentId: ID
  ): AuditLog

  get_audit_logs_count(
    action: String
    dateRange: DateRangeInput
    orderId: ID
    paginateData: PaginateDataInput
    paymentId: ID
  ): Int
}
