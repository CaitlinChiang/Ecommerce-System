type WebsiteText {
  _id: ID
  content: String
  type: String
  updatedAt: String
}

extend type Query {
  get_website_text(type: String!): WebsiteText
}

extend type Mutation {
  update_website_text(content: String!, type: String!): WebsiteText
}
