type AnalyticsOrdersCount {
  date: String
  orders: Int
}

type AnalyticsRevenueCount {
  date: String
  revenue: Float
}

extend type Query {
  get_analytics_orders_count(dateRange: DateRangeInput!): AnalyticsOrdersCount

  get_analytics_revenue_count(dateRange: DateRangeInput!): AnalyticsRevenueCount
}
