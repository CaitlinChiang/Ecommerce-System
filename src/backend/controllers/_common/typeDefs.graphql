scalar Upload

type File {
  filename: String!
  mimetype: String!
  encoding: String!
}

type DeliveryAddress {
  address: String
  cityId: ID
}
input DeliveryAddressInput {
  address: String
  cityId: ID
}

input DateRangeInput {
  startDate: String!
  endDate: String!
}

input PaginateTableInput {
  page: Int!
  rowsPerPage: Int!
  searchText: String
  sortBy: String!
  sortDirection: String!
}
