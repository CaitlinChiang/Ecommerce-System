type Product {
  _id: ID
  image: Upload
  imageUrl: String
  name: String
  price: Float
  type: String
  createdAt: Date
  updatedAt: Date
}

extend type Query {
  get_product(_id: ID!): Product
  get_products: [Product]
  get_products_count: Int
}

extend type Mutation {
  create_product(
    image: Upload
    name: String!
    price: Float!
    type: String!
  ): Product

  update_product(
    _id: ID!
    image: Upload
    imageUrl: String
    name: String!
    price: Float!
    type: String!
  ): Product

  delete_product(
    _id: ID!
    imageUrl: String
  ): Product
}
